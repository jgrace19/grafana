import * as stylex from '@stylexjs/stylex';

export const metricComboboxStyles = stylex.create({
  wrapper: {
    display: 'flex',
  },
  button: {
    borderTopLeftRadius: 'unset',
    borderBottomLeftRadius: 'unset',
  },
});
