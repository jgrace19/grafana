import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const operationParamEditorRegistryStyles = stylex.create({
  optionalParam: {
    marginTop: grafanaTokens.spacing_x1,
  },
});
