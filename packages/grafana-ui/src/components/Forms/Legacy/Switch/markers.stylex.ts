import * as stylex from '@stylexjs/stylex';

/** On the legacy Switch's hidden checkbox: checking it moves and colours the slider next to it. */
export const legacySwitchInputMarker = stylex.defineMarker();
