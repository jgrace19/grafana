import * as stylex from '@stylexjs/stylex';

/** On FileUpload's hidden file input: focusing it shows the focus ring on the button-styled label. */
export const fileInputMarker = stylex.defineMarker();
