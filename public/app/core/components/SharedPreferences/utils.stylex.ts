import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const utilsStyles = stylex.create({
  labelText: {
    marginRight: '6px',
  },
  form: {
    width: '100%',
          maxWidth: '600px',
  },
});
