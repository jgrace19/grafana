import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const annotationSettingsListStyles = stylex.create({
  table: {
    width: '100%',
        overflowX: 'auto',
  },
});
