import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const publicDashboardBadgeStyles = stylex.create({
  badge: {
    color: 'grey',
        backgroundColor: 'transparent',
        border: '1px solid',
  },
});
