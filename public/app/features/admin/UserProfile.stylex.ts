import * as stylex from '@stylexjs/stylex';

export const userProfileStyles = stylex.create({
  label: {
    fontWeight: 500,
  },
  lockMessage: {
    fontStyle: 'italic',
    marginRight: '0.6rem',
  },
});
