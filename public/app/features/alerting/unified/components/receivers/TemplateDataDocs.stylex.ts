import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const templateDataDocsStyles = stylex.create({
  interactiveType: {
    color: grafanaTokens.colors_text_link,
  },
});
