import * as stylex from '@stylexjs/stylex';

import { themeSpacing } from '../../../stylex/spacing';

export const ruleDetailsDataSourcesStyles = stylex.create({
  dataSourceIcon: {
    width: themeSpacing(2),
    height: themeSpacing(2),
  },
});
