import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const ruleInspectorStyles = stylex.create({
  subtitle: {
    display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
  },
});
