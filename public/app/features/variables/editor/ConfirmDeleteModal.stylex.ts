import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const confirmDeleteModalStyles = stylex.create({
  modal: {
    width: 'max-content',
        maxWidth: '80vw',
  },
});
