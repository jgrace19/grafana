import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const panelLibraryOptionsGroupStyles = stylex.create({
  libraryPanelsView: {
    width: '100%',
  },
});
