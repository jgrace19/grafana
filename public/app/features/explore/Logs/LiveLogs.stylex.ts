import * as stylex from '@stylexjs/stylex';

/** Background of a freshly streamed row: `theme.colors.info.transparent` at 25% alpha, set by LiveLogs. */
export const liveLogsVars = stylex.defineVars({
  '--gf-live-logs-fresh-row': 'transparent',
});
