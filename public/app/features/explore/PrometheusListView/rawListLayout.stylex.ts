import * as stylex from '@stylexjs/stylex';

/** Column geometry shared by the raw Prometheus list's rows, value cells and value headings. */
export const rawListLayout = stylex.defineConsts({
  columnWidth: '80px',
  copyIconSpace: '25px',
  lineEndSpace: '20px',
});
