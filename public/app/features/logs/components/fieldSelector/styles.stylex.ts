import * as stylex from '@stylexjs/stylex';

export const logsFieldSelectorWrapperStylesX = stylex.create({
  collapsedButtonContainer: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'center',
    paddingTop: 2,
  },
  collapsedButton: {
    margin: 0,
  },
});
