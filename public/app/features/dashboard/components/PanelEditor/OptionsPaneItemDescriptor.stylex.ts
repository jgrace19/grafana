import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const optionsPaneItemDescriptorStyles = stylex.create({
  container: {
    display: 'flex',
          justifyContent: 'space-between',
  },
});
