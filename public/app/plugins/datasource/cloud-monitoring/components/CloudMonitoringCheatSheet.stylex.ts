import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const cloudMonitoringCheatSheetStyles = stylex.create({
  inline0: {
    listStyle: 'none',
  },
});
