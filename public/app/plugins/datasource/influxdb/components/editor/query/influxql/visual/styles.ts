import { css } from '@emotion/css';

// stylex: pending Select migration. Select's container sets its own padding in Emotion, which beats a StyleX override.
export const paddingRightClass = css({
  paddingRight: '4px',
});
