import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const liveChannelEditorStyles = stylex.create({
  dropWrap: {
    marginBottom: themeSpacing(1),
  },
});
