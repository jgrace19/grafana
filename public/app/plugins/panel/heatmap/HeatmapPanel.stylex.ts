import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const heatmapPanelStyles = stylex.create({
  colorScaleWrapper: {
    marginLeft: '25px',
        padding: '10px 0',
        maxWidth: '300px',
  },
});
