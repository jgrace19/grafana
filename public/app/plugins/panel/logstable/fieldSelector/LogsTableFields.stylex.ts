import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const logsTableFieldsStyles = stylex.create({
  fieldSelectorWrapper: {
    position: 'absolute',
          height: height,
          width: fieldSelectorWidth,
          zIndex: 1,
  },
});
