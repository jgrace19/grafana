import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const logsTableStyles = stylex.create({
  wrapper: {
    // prevent overflow in the tiny suggestions preview
          overflow: 'hidden',
          height,
          width,
  },
});
