import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const treeNavigationEditorStyles = stylex.create({
  addLayerButton: {
    marginLeft: '18px',
        minWidth: '150px',
  },
});
