import * as stylex from '@stylexjs/stylex';

import { grafanaTokens } from '@grafana/ui/unstable';

export const gaugePanelStyles = stylex.create({
  root: {
    cursor: 'context-menu',
        background: 'none',
        border: 'none',
  },
});
