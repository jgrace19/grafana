apiVersion: 1

providers:
  - name: visual-baseline
    orgId: 1
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 3600
    options:
      path: ${VB_DASHBOARDS_DIR}
      foldersFromFilesStructure: true
